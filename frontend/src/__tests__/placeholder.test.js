import { describe, it, expect } from 'vitest'

// Wiring check for the test runner; replaced by real tests alongside the app UI.
describe('test runner', () => {
  it('runs', () => {
    expect(true).toBe(true)
  })
})
