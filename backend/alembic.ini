# Minimal alembic config. The database URL is built from the app-db env
# contract (DB_HOST/DB_PORT/DB_NAME/DB_USER/DB_PASSWORD) in alembic/env.py —
# never hardcoded here.
[alembic]
script_location = %(here)s/alembic
