# 007 — PWA: manifest, service worker, icons, push UI

`manifest.webmanifest` + 192/512/maskable icons, hand-rolled `sw.js` (app-shell
caching, push + notificationclick handlers), SW registration, Settings push
toggle with iOS install-first explainer. Verify installability + a real push
round-trip on staging.
