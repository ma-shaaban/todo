# 008 — Activity feed + owner docs + polish

Activity table + per-space feed ("Ana completed *Buy milk*"), overdue chips,
owner-facing TechDocs rewrite (what the app does, reminders, PWA install incl.
iOS steps), `docs/google-signin-setup.md` with exact GCP steps + redirect URIs,
README refresh.
