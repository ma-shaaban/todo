# 006 — Frontend app (auth, spaces, todos, my tasks, invites, notifications)

Replace the demo App.jsx: react-router 8, login/signup, spaces list/detail
(todos + members tabs), todo editor sheet (due, priority, assignee, recurrence,
reminder presets), My Tasks, invite landing `/invite/:code` (works pre-auth),
notifications page + bell polling, settings. Mobile-first hand-rolled CSS,
dark mode. Vitest for `format.js` date/label helpers + `api.js` client.
